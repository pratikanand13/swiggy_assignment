Packages used: 
    1. jest : for writing unit tests for the application.
    2. readline : Node.js's module for reading user input.

Steps to run the program :

    1. Install all the dependencies

        `npm i`
    
    2. To run the unit tests, 

        `npx jest`
    
    3. To run the program,

        `node app.js`

        and follow the CLI to interact with the arena. 

Steps:
        To use ES ,we have to add   "type": "module" in package.json as latest js versions requires 'require'
        Created a Player (used constructor to intialise the object)
        Created an Arena class (this class contains constructor)
                -constructor to initial

